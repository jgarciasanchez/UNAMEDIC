<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Salud.Views.DiabetesHistoryPage"
             Title="Historial">
    <ContentPage.Content>
        <StackLayout BindingContext="{Binding Diabetes}">
            <ListView
                HasUnevenRows="True"
                SeparatorVisibility="None"
                IsPullToRefreshEnabled="False" 
                ItemsSource ="{Binding diabetes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell >
                            <!--<ViewCell.ContextActions>
                                <MenuItem Text="Ver facturas" Command="{Binding evtViewInvoice}" CommandParameter="{x:Reference invoiceCustomerPage}"/>
                            </ViewCell.ContextActions>-->
                            <Frame CornerRadius="10" Margin="3" >
                                <Grid Margin="-15" Padding="0">
                                    <!--<Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding evtViewInvoice}" CommandParameter="{x:Reference invoiceCustomerPage}"/>
                                    </Grid.GestureRecognizers>-->
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="50"/>
                                    </Grid.RowDefinitions>
                                    <Label Text="{Binding Glucosa}"/>
                                    <Label Grid.Column="1" Text="{Binding Peso}"/>
                                    <Label Grid.Column="2" Text="{Binding Nota}"/>
                                    <Label Grid.Row="1"  Grid.Column="2" Text="{Binding Hora}"/>
                                    <Label Grid.Row="1" Text="{Binding Fecha}"/>
                                </Grid>
                            </Frame>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>