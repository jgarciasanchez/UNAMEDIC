<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Salud.Views.LoginPage"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout BindingContext="{Binding Login}" Padding="20,50,20,0" BackgroundColor="Azure">
            <Image Source="UNAMEDIC" />
            <Entry Placeholder="Usuario" TextColor="Gray"/>
            <Entry Placeholder="Contraseña" IsPassword="True" TextColor="Gray"/>
            <StackLayout Margin="0,20,0,0">
                <Button VerticalOptions="Center" Text="Iniciar Sesión" Command="{Binding LoginCommand}"/>
                <Button VerticalOptions="Center" BackgroundColor="Transparent" TextColor="Blue" Text="Registro" Command="{Binding SingInCommand}"/>
            </StackLayout>
            <Button VerticalOptions="EndAndExpand" BackgroundColor="Transparent" TextColor="Blue" Text="Contáctenos"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>