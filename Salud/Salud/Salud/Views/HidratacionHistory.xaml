<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:syncfusion="clr-namespace:Syncfusion.SfCalendar.XForms;assembly=Syncfusion.SfCalendar.XForms"
             xmlns:local="clr-namespace:Salud.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Salud.Views.HidratacionHistory"
             
             Title="Historial">
    <ContentPage.Content>
        <StackLayout>
            <syncfusion:SfCalendar  x:Name="calendar" ViewMode="MonthView" ShowInlineEvents="True" 
                                   InlineViewMode="Inline" MaximumEventIndicatorCount="1" 
                                    SelectedDate="{Binding Fecha}"
                                   DataSource="{Binding Source={x:Static local:CalendarViewModel.CalendarInlineEventsWater}}">
                <syncfusion:SfCalendar.MonthViewSettings>
                    <syncfusion:MonthViewSettings>
                        <syncfusion:MonthViewSettings.InlineItemTemplate>
                            <DataTemplate>
                                <Button BackgroundColor="Aqua" Text="{Binding Subject}" TextColor="White" />
                            </DataTemplate>
                        </syncfusion:MonthViewSettings.InlineItemTemplate>
                    </syncfusion:MonthViewSettings>
                </syncfusion:SfCalendar.MonthViewSettings>
            </syncfusion:SfCalendar>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>